server:
  port: ${SERVER_PORT:8092}

spring:
  application:
    name: ENEMY
  main:
    banner-mode: off

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}
