server:
  port: ${SERVER_PORT:8090}

spring:
  application:
    name: GATEWAY

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    preferIpAddress: true
